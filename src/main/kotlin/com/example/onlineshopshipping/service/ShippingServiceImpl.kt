package com.example.onlineshopshipping.service

import org.springframework.stereotype.Service


@Service
class ShippingServiceImpl : ShippingService {
    override fun startShipping(orderId: Long) {
        return
    }

    override fun finishShipping(orderId: Long) {
        return
    }
}